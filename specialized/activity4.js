let celsiusValue
let fahrenheltValue
let userChoice

userChoice = 1
celsiusValue = 0.0
if (userChoice == 1)
   console. log ("fahrenheltValue(celsiusValue * 9.0 / 5.0) + 32.0")
console. log (celsiusValue)
console. log ("C is")
console. log (fahrenheltValue)
console. log ("F\n") 
celsiusValue = celsiusValue + 5
userChoice = window.prompt("25")
console. log ("Goodbye.")